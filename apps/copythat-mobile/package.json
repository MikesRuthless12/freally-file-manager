{
  "name": "copythat-mobile",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Phase 37 — Copy That mobile companion. Vite + Svelte 5 PWA that connects to the desktop's PeerJS peer-id over WebRTC and drives the full /control surface (job pause/resume/cancel, collision resolution, history rerun, secure delete, live globals stats). Distributed as a PWA — users install via Add to Home Screen, no App Store gate.",
  "scripts": {
    "dev": "vite",
    "build": "svelte-check && vite build",
    "preview": "vite preview",
    "svelte-check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "peerjs": "^1.5.4"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^0.21.0"
  }
}
