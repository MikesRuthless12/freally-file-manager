<?xml version="1.0" encoding="UTF-8"?>
<!--
  NSServices fragment for Copy That v1.0.0.

  Phase 16 packaging merges this array under the `NSServices` key in
  the main app's `Copy That v1.0.0.app/Contents/Info.plist`. When the
  OS notices the app, the two entries appear under every app's
  "Services" submenu whenever the current pasteboard selection is a
  file path.

  The NSMessage strings (`copyFiles` / `moveFiles`) map 1:1 to the
  @objc selectors in `CopyThatServiceHandler.swift`.

  Reference: macOS "Services Implementation Guide"
  https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/SysServices/introduction.html
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<array>
    <dict>
        <key>NSMenuItem</key>
        <dict>
            <key>default</key>
            <string>Copy with Copy That</string>
        </dict>
        <key>NSMessage</key>
        <string>copyFiles</string>
        <key>NSPortName</key>
        <string>Copy That v1.0.0</string>
        <key>NSRequiredContext</key>
        <dict>
            <!--
                No text-content requirement beyond "one or more file
                paths on the pasteboard". NSSendTypes below filters.
            -->
        </dict>
        <key>NSReturnTypes</key>
        <array/>
        <key>NSSendTypes</key>
        <array>
            <string>public.file-url</string>
            <string>NSFilenamesPboardType</string>
        </array>
    </dict>
    <dict>
        <key>NSMenuItem</key>
        <dict>
            <key>default</key>
            <string>Move with Copy That</string>
        </dict>
        <key>NSMessage</key>
        <string>moveFiles</string>
        <key>NSPortName</key>
        <string>Copy That v1.0.0</string>
        <key>NSRequiredContext</key>
        <dict/>
        <key>NSReturnTypes</key>
        <array/>
        <key>NSSendTypes</key>
        <array>
            <string>public.file-url</string>
            <string>NSFilenamesPboardType</string>
        </array>
    </dict>
</array>
</plist>
